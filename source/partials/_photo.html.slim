- layout = locals[:layout] ? layout : ""

.photo-wrapper[class="photo-#{layout}"]
  .card
    .card-image style="background-image: url('#{post_image_path image}')"
    .card-text
      = yield
  = link_to "", post_image_path(image), class: "fancybox"
